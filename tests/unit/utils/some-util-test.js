import someUtil from 'typescript-app/utils/some-util';
import { module, test } from 'qunit';

module('Unit | Utility | some util');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = someUtil();
  assert.ok(result);
});
